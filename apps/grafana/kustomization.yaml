apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - oauth-client.yaml
  - oauth-secret.yaml
  - grafana.yaml
  - datasource-prometheus.yaml
  - dashboard-argocd.yaml
  # Comment out any resource to exclude it from deployment
