apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - grafana-operator.yaml
  - vault-secrets-operator.yaml
  # - cluster-logging.yaml   # uncomment when cluster-logging is needed
  # Comment out any operator to exclude it
