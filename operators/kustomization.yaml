apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - argocd-operator.yaml
  - cert-manager.yaml
  - cluster-logging.yaml
  - grafana-operator.yaml
  - vault-secrets-operator.yaml
  # Comment out any operator to exclude it
